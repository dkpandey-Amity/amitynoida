import { CleanHtmlptagPipe } from './clean-htmlptag.pipe';

describe('CleanHtmlptagPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanHtmlptagPipe();
    expect(pipe).toBeTruthy();
  });
});
